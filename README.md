## 📝 License

This project is licensed under the MIT License - see the LICENSE file for details.

### MIT License Summary

**Permissions:**
- ✅ Commercial use
- ✅ Modification
- ✅ Distribution
- ✅ Private use

**Conditions:**
- 📄 License and copyright notice must be included

**Limitations:**
- ❌ No liability
- ❌ No warranty

### Third-Party Licenses

This project uses several open-source libraries with their respective licenses:

**Computer Vision & AI:**
- **OpenCV** - Apache 2.0 License
- **PyTorch** - BSD-3-Clause License
- **Ultralytics YOLO** - AGPL-3.0 License
- **NumPy** - BSD License
- **Pillow** - PIL Software License

**Web Framework:**
- **Flask** - BSD-3-Clause License
- **Flask-SocketIO** - MIT License
- **eventlet** - MIT License

**Camera Control:**
- **onvif-zeep** - MIT License
- **zeep** - MIT License

**Streaming:**
- **yt-dlp** - Unlicense

**System Utilities:**
- **psutil** - BSD-3-Clause License
- **requests** - Apache 2.0 License

### Commercial Use

This software is free for commercial use under the MIT License, but please note:

**YOLO Models:** The Ultralytics YOLO library uses AGPL-3.0 license. For commercial use of YOLO models, consider:
- Using the models under AGPL-3.0 terms (open source your application)
- Purchasing a commercial license from Ultralytics
- Using alternative object detection models with more permissive licenses

**Camera Integration:** Ensure compliance with camera manufacturer's SDK licenses and terms of service.

### Attribution

While not required by the MIT License, attribution is appreciated:

```
Multi-Source CCTV System
https://github.com/your-repo/multi-source-cctv
Licensed under MIT License
```

## 🙏 Acknowledgments

### Core Technologies
- **[Ultralytics](https://ultralytics.com/)** - YOLO implementation and pre-trained models
- **[OpenCV](https://opencv.org/)** - Computer vision library for video processing
- **[Flask](https://flask.palletsprojects.com/)** - Web framework for the user interface
- **[PyTorch](https://pytorch.org/)** - Deep learning framework for AI inference

### Camera Standards
- **[ONVIF](https://www.onvif.org/)** - Open Network Video Interface Forum for PTZ control
- **[RTSP Protocol](https://tools.ietf.org/html/rfc2326)** - Real Time Streaming Protocol specification

### Video Processing
- **[FFmpeg](https://ffmpeg.org/)** - Multimedia framework for video codec support
- **[yt-dlp](https://github.com/yt-dlp/yt-dlp)** - Video extraction for live streaming platforms

### Development Tools
- **[GitHub](https://github.com/)** - Source code hosting and collaboration
- **[Docker](https://www.docker.com/)** - Containerization platform
- **[Nginx](https://nginx.org/)** - Web server for production deployment

### Community Contributors
- **Bug reporters** - Users who identified and reported issues
- **Feature requesters** - Community members who suggested improvements
- **Code contributors** - Developers who submitted pull requests
- **Documentation writers** - Contributors who improved guides and documentation
- **Testers** - Users who tested across different platforms and configurations

### Special Thanks
- **Security researchers** - For responsible disclosure of security issues
- **Performance optimizers** - For identifying and fixing bottlenecks
- **Cross-platform testers** - For ensuring compatibility across operating systems
- **Accessibility advocates** - For making the interface more inclusive

### Inspiration
This project was inspired by the need for:
- **Affordable surveillance solutions** for small businesses and homes
- **Open-source alternatives** to expensive commercial CCTV systems
- **AI-powered detection** accessible to non-technical users
- **Multi-platform compatibility** across different devices and cameras

### Research and Papers
- **YOLO (You Only Look Once)** - Object detection research papers
- **Computer Vision algorithms** - Academic research in real-time video processing
- **Network streaming protocols** - Studies on low-latency video transmission
- **Edge computing** - Research on running AI inference on resource-constrained devices

## 📧 Support

### 🆘 Getting Help

**Before Seeking Help:**
1. ✅ Check the [Troubleshooting Guide](#-troubleshooting)
2. ✅ Search [existing GitHub issues](https://github.com/your-repo/multi-source-cctv/issues)
3. ✅ Review the [documentation](#-quick-start) thoroughly
4. ✅ Try with a minimal configuration to isolate the problem

### 📋 Reporting Issues

**🐛 Bug Reports:**
Create a [new issue](https://github.com/your-repo/multi-source-cctv/issues/new?template=bug_report.md) with:

```markdown
**System Information:**
- OS: [e.g., Windows 11, Ubuntu 22.04]
- Python Version: [e.g., 3.9.7]
- Browser: [e.g., Chrome 118.0]
- GPU: [e.g., NVIDIA RTX 3070, Intel integrated]

**Installation Method:**
- [ ] Windows install.bat
- [ ] Linux install.sh
- [ ] Manual pip installation
- [ ] Docker
- [ ] Other: ___________

**Expected Behavior:**
Clear description of what should happen

**Actual Behavior:**
Clear description of what actually happens

**Steps to Reproduce:**
1. Go to '...'
2. Click on '...'
3. Enter '...'
4. See error

**Error Messages:**
```
Paste full error messages here
```

**Additional Context:**
- Camera model/brand (if applicable)
- Network configuration (if applicable)
- Any custom configurations
```

**✨ Feature Requests:**
Create a [feature request](https://github.com/your-repo/multi-source-cctv/issues/new?template=feature_request.md) with:
- **Problem description** - What problem does this solve?
- **Proposed solution** - How should it work?
- **Alternatives considered** - What other options did you consider?
- **Use cases** - Who would benefit from this feature?

### 💬 Community Support

**GitHub Discussions:**
- [General Q&A](https://github.com/your-repo/multi-source-cctv/discussions/categories/q-a)
- [Show and Tell](https://github.com/your-repo/multi-source-cctv/discussions/categories/show-and-tell)
- [Ideas](https://github.com/your-repo/multi-source-cctv/discussions/categories/ideas)

**Community Guidelines:**
- 🤝 Be respectful and constructive
- 🔍 Search before posting duplicate questions
- 📝 Provide sufficient detail in your questions
- 🙏 Thank community members who help you
- 💡 Share solutions that worked for you

### 🏢 Professional Support

**Consulting Services:**
For organizations requiring professional support:
- **Installation and setup assistance**
- **Custom feature development**
- **Performance optimization**
- **Security auditing**
- **Training and documentation**

**Contact:** Create an issue with the "professional-support" label for consulting inquiries.

### 🌍 International Support

**Language Support:**
- **English** - Primary language for documentation and support
- **Community translations** - Welcome contributions for documentation in other languages
- **Local communities** - Regional user groups and forums (see Discussions)

**Time Zone Coverage:**
- **Americas** - GitHub issues and discussions
- **Europe** - GitHub issues and discussions  
- **Asia-Pacific** - GitHub issues and discussions

### 📚 Additional Resources

**Learning Resources:**
- **[YOLO Documentation](https://docs.ultralytics.com/)** - Official YOLO training and usage
- **[OpenCV Tutorials](https://docs.opencv.org/master/d9/df8/tutorial_root.html)** - Computer vision fundamentals
- **[Flask Documentation](https://flask.palletsprojects.com/)** - Web development with Flask
- **[ONVIF Specifications](https://www.onvif.org/specs/)** - Camera control standards

**Video Tutorials:**
- **YouTube Channel** - Step-by-step setup and configuration videos
- **Demo Videos** - Feature demonstrations and use cases
- **Webinars** - Live Q&A sessions and advanced topics

**Blog Posts:**
- **Setup Guides** - Detailed installation walkthroughs
- **Use Cases** - Real-world deployment scenarios
- **Performance Tips** - Optimization best practices
- **Security Guides** - Hardening and protection strategies

### ⭐ Show Your Support

If this project helps you, please consider:
- ⭐ **Star the repository** on GitHub
- 🍴 **Fork and contribute** improvements
- 📢 **Share with others** who might benefit
- 📝 **Write a blog post** about your experience
- 💬 **Join discussions** and help other users
- 📧 **Provide feedback** on features and usability

### 📞 Emergency Support

**Critical Security Issues:**
For security vulnerabilities, please email: security@your-domain.com
- Use PGP encryption if possible
- Include proof-of-concept if available
- Allow reasonable time for response before public disclosure

**Service Outages:**
For production deployment issues affecting business operations:
1. Check [system status page](https://status.your-domain.com) (if available)
2. Create high-priority GitHub issue with "urgent" label
3. Include impact assessment and business context

---

**Built with ❤️ by the open-source community for real-time AI-powered video surveillance**## 🤝 Contributing

We welcome contributions to the Multi-Source CCTV System! Here's how you can help improve the project:

### 🚀 Getting Started

**1. Fork and Clone**
```bash
# Fork the repository on GitHub, then clone your fork
git clone https://github.com/YOUR_USERNAME/multi-source-cctv.git
cd multi-source-cctv

# Add upstream remote
git remote add upstream https://github.com/ORIGINAL_OWNER/multi-source-cctv.git
```

**2. Set Up Development Environment**
```bash
# Linux/macOS
python3 -m venv dev_env
source dev_env/bin/activate

# Windows
python -m venv dev_env
dev_env\Scripts\activate

# Install development dependencies
pip install -r requirements.txt
pip install pytest black flake8 isort mypy pre-commit
```

**3. Install Pre-commit Hooks**
```bash
pre-commit install
# This ensures code formatting and linting on every commit
```

### 🎯 Ways to Contribute

**🐛 Bug Reports**
- Use GitHub Issues to report bugs
- Include system information, error messages, and steps to reproduce
- Check existing issues before creating new ones

**✨ Feature Requests**
- Suggest new features via GitHub Issues
- Describe the problem your feature would solve
- Provide examples of how it would work

**📝 Code Contributions**
- Fix bugs, add features, improve documentation
- Follow the coding standards and testing guidelines
- Submit pull requests with clear descriptions

**📖 Documentation**
- Improve README, add tutorials, fix typos
- Add code comments and docstrings
- Create video tutorials or blog posts

**🧪 Testing**
- Add test cases for new features
- Improve test coverage
- Test on different platforms and configurations

### 📋 Development Guidelines

**Code Style:**
```bash
# Format code with black
black app.py

# Sort imports with isort
isort app.py

# Lint with flake8
flake8 app.py

# Type checking with mypy
mypy app.py
```

**Commit Messages:**
```
feat: add support for ONVIF preset positions
fix: resolve RTSP connection timeout issues
docs: update installation guide for Ubuntu 22.04
test: add unit tests for YOLO model detection
refactor: improve error handling in camera connection
```

**Branch Naming:**
```
feature/onvif-presets
bugfix/rtsp-timeout
docs/ubuntu-guide
test/yolo-detection
refactor/error-handling
```

### 🧪 Testing

**Run Tests:**
```bash
# Run all tests
pytest

# Run specific test file
pytest tests/test_detection.py

# Run with coverage
pytest --cov=app tests/

# Run integration tests
pytest tests/integration/
```

**Write Tests:**
```python
# Example test structure
def test_yolo_model_loading():
    """Test YOLO model loads correctly"""
    model = load_yolo_model('yolov8n.pt')
    assert model is not None
    assert model.device.type in ['cpu', 'cuda']

def test_rtsp_connection():
    """Test RTSP connection with mock camera"""
    with mock_rtsp_server() as server:
        result = connect_rtsp(server.url)
        assert result.success is True
```

### 🔄 Pull Request Process

**1. Create Feature Branch**
```bash
git checkout -b feature/your-feature-name
git push -u origin feature/your-feature-name
```

**2. Make Changes**
- Write clean, documented code
- Add tests for new functionality
- Update documentation as needed

**3. Test Your Changes**
```bash
# Run tests
pytest

# Test with different Python versions
tox

# Test installation process
./install.sh  # Linux
install.bat   # Windows
```

**4. Submit Pull Request**
- Create PR against main branch
- Fill out PR template completely
- Link related issues
- Request reviews from maintainers

**PR Template:**
```markdown
## Description
Brief description of changes

## Type of Change
- [ ] Bug fix
- [ ] New feature
- [ ] Documentation update
- [ ] Performance improvement
- [ ] Code refactoring

## Testing
- [ ] All tests pass
- [ ] Added tests for new functionality
- [ ] Tested on multiple platforms
- [ ] Manual testing completed

## Screenshots (if applicable)
Add screenshots of UI changes

## Checklist
- [ ] Code follows style guidelines
- [ ] Self-review completed
- [ ] Documentation updated
- [ ] No breaking changes (or clearly documented)
```

### 🏗️ Development Setup

**Development Dependencies:**
```txt
# Code quality
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0
mypy>=1.0.0
pre-commit>=3.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0

# Documentation
sphinx>=6.0.0
sphinx-rtd-theme>=1.2.0

# Development utilities
ipython>=8.0.0
jupyter>=1.0.0
```

**IDE Configuration:**

**VS Code (`.vscode/settings.json`):**
```json
{
    "python.defaultInterpreterPath": "./dev_env/bin/python",
    "python.linting.enabled": true,
    "python.linting.flake8Enabled": true,
    "python.formatting.provider": "black",
    "python.sortImports.provider": "isort",
    "python.testing.pytestEnabled": true
}
```

**PyCharm:**
- Configure interpreter: dev_env/bin/python
- Enable pytest as test runner
- Configure black as code formatter
- Enable flake8 inspections

### 🌟 Recognition

**Contributors Hall of Fame:**
- All contributors are recognized in CONTRIBUTORS.md
- Significant contributions get mentioned in release notes
- Top contributors may be invited as maintainers

**Types of Recognition:**
- 🐛 Bug hunters - Finding and reporting critical bugs
- ✨ Feature creators - Adding valuable new features
- 📖 Documentation heroes - Improving docs and guides
- 🧪 Test masters - Improving test coverage
- 🔧 Code quality champions - Refactoring and optimization
- 🌍 Community builders - Helping other users

### 📞 Getting Help

**Development Questions:**
- GitHub Discussions for general questions
- Discord/Slack for real-time chat (if available)
- Email maintainers for private concerns

**Before Asking:**
1. Check existing documentation
2. Search GitHub issues and discussions
3. Try to reproduce with minimal example
4. Include relevant system information

### 🎯 Priority Areas

**High Priority:**
- 🐛 Bug fixes for critical functionality
- 🔒 Security improvements
- 📱 Mobile interface enhancements
- ⚡ Performance optimizations

**Medium Priority:**
- ✨ New camera brand support
- 🎨 UI/UX improvements
- 📊 Advanced analytics features
- 🔧 Configuration management

**Low Priority:**
- 🎭 Theme customization
- 🔌 Plugin system
- 📈 Advanced reporting
- 🌐 Multi-language support

### 📄 Licensing

By contributing to this project, you agree that your contributions will be licensed under the same license as the project (MIT License). Make sure you have the right to contribute any code or content you submit.# 🚀 Multi-Source CCTV System with AI Detection

## 📖 Overview

A comprehensive, real-time video surveillance system that supports multiple video sources with AI-powered object detection. Built with Python Flask, OpenCV, and YOLO, featuring ultra-low latency streaming, PTZ camera control, and toggleable detection overlays.

## ✨ Key Features

### 📡 Multi-Source Video Support
- **RTSP/IP Cameras** with enhanced URL parsing
  - Supports all standard RTSP formats
  - Auto-credential extraction from URLs
  - Smart IP detection for ONVIF PTZ control
  - Brand-specific templates (Hikvision, Dahua, Axis)
- **USB/Built-in Webcams** with auto-detection
- **Live Streaming URLs** (YouTube Live, Twitch, HLS, Facebook Live, Instagram Live)
- **Video Files** (MP4, AVI, MOV, etc.) with proper timing preservation
- **HTTP/HTTPS Direct Streams**
- **DASH Adaptive Streams**

### 🤖 Universal AI Detection System
- **Official YOLO Models**: yolo11n.pt, yolov8n.pt, yolov5n.pt, etc.
- **Custom .pt Models**: Supports ANY PyTorch .pt file
- **Smart Model Detection**: Auto-finds and prioritizes best available models
- **Cross-Platform Discovery**: Works on Windows, Linux, macOS
- **GPU Acceleration**: CUDA support when available
- **Real-time Object Detection**: Person tracking and counting
- **Toggleable Detection Overlay**: Show/hide bounding boxes while keeping detection active

### ⏱️ Advanced Timing Preservation
- **Live Stream Mode**: Preserves original FPS and timing for live streams
- **File Mode**: Maintains natural playback speed for video files
- **Auto-Detection**: Automatically identifies live streams vs files
- **Fixed Timing Issues**: Resolves fast playback problems
- **Source-Specific Optimization**: Different strategies for each source type

### 🔒 Enhanced RTSP Support
- **Smart URL Parsing**: Handles all standard RTSP URL formats
  - `rtsp://192.168.1.4:554/live/ch00_0` (No auth)
  - `rtsp://admin:@192.168.1.4:554/live/ch00_0` (User only)
  - `rtsp://admin:admin@192.168.1.4:554/live/ch00_0` (Full auth)
- **Auto-Credential Extraction**: Parses username/password from URLs
- **Fallback Authentication**: Uses separate credential fields if URL doesn't contain them
- **IP Auto-Detection**: Extracts camera IP for ONVIF PTZ control
- **Error Notifications**: Real-time RTSP connection error alerts

### 🕹️ PTZ Camera Control
- **Smart ONVIF Discovery**: Multi-port, multi-authentication discovery
- **Pan/Tilt/Zoom Control**: Full PTZ functionality
- **Auto Person Tracking**: AI-powered person following
- **Adjustable Sensitivity**: Customizable tracking parameters
- **Speed Control**: Variable PTZ movement speeds

### ⚡ Ultra-Low Latency Streaming
- **Threaded Architecture**: Separated capture and streaming threads
- **Minimal Buffering**: Optimized for real-time performance
- **Source-Specific Optimization**: Different strategies per source type
- **Performance Monitoring**: Real-time FPS and latency tracking

### 🌐 Modern Web Interface
- **Responsive Design**: Works on desktop and mobile
- **Real-time Controls**: Live adjustment of all parameters
- **Source Templates**: Pre-configured settings for popular services
- **Fullscreen Support**: Immersive video viewing
- **Performance Dashboard**: Live system monitoring

## 🛠️ Installation

### System Requirements
- **Python**: 3.8 or higher
- **RAM**: 4GB minimum, 8GB recommended
- **Storage**: 2GB free space (for models and dependencies)
- **Network**: Internet connection for model downloads

### 🐧 Linux Installation

#### Ubuntu/Debian
```bash
# Update system packages
sudo apt update && sudo apt upgrade -y

# Install Python and pip
sudo apt install python3 python3-pip python3-venv -y

# Install system dependencies for OpenCV
sudo apt install python3-opencv libopencv-dev -y
sudo apt install libglib2.0-0 libsm6 libxext6 libxrender-dev libgomp1 -y

# Install additional dependencies for video processing
sudo apt install ffmpeg libavcodec-extra -y

# Create virtual environment (recommended)
python3 -m venv cctv_env
source cctv_env/bin/activate

# Install Python packages
pip install --upgrade pip
pip install opencv-python flask flask-socketio onvif-zeep numpy ultralytics yt-dlp

# Optional: PyTorch with CUDA (for GPU acceleration)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
```

#### CentOS/RHEL/Fedora
```bash
# CentOS/RHEL
sudo yum update -y
sudo yum install python3 python3-pip python3-devel -y
sudo yum install opencv opencv-python opencv-devel -y
sudo yum install ffmpeg ffmpeg-devel -y

# Fedora
sudo dnf update -y
sudo dnf install python3 python3-pip python3-devel -y
sudo dnf install opencv opencv-python opencv-devel -y
sudo dnf install ffmpeg ffmpeg-devel -y

# Create virtual environment and install packages
python3 -m venv cctv_env
source cctv_env/bin/activate
pip install --upgrade pip
pip install opencv-python flask flask-socketio onvif-zeep numpy ultralytics yt-dlp
```

#### Arch Linux
```bash
# Update system
sudo pacman -Syu

# Install dependencies
sudo pacman -S python python-pip python-virtualenv
sudo pacman -S opencv python-opencv
sudo pacman -S ffmpeg

# Create virtual environment
python -m venv cctv_env
source cctv_env/bin/activate
pip install opencv-python flask flask-socketio onvif-zeep numpy ultralytics yt-dlp
```

### 🪟 Windows Installation

#### Method 1: Automated Installation (Recommended)
```cmd
# Download install.bat from the repository
# Right-click install.bat and select "Run as administrator"
# Follow the interactive installer prompts

# The installer will:
# 1. Check system requirements
# 2. Create virtual environment
# 3. Install all dependencies
# 4. Configure Windows Firewall
# 5. Download YOLO models (optional)
# 6. Create startup scripts
```

**Installation Options:**
- **🚀 Quick Install**: CPU-only (works on all systems)
- **🔥 GPU Install**: NVIDIA CUDA support with GPU acceleration
- **🔧 Development Install**: Includes debugging and development tools
- **🎯 Custom Install**: Choose specific components to install

#### Method 2: Manual Installation
```cmd
# Download and install Python 3.8+ from python.org
# Make sure to check "Add Python to PATH" during installation

# Open Command Prompt or PowerShell as Administrator
# Update pip
python -m pip install --upgrade pip

# Install Microsoft Visual C++ Build Tools (required for some packages)
# Download from: https://visualstudio.microsoft.com/visual-cpp-build-tools/

# Create virtual environment (recommended)
python -m venv cctv_env
cctv_env\Scripts\activate

# Install core dependencies
pip install opencv-python>=4.8.0 flask>=2.3.0 flask-socketio>=5.3.0
pip install onvif-zeep>=0.2.12 numpy>=1.24.0 ultralytics>=8.0.0
pip install yt-dlp>=2023.7.6 psutil>=5.9.0 requests>=2.31.0
pip install pywin32>=306 eventlet>=0.33.0 zeep>=4.2.0 Pillow>=10.0.0

# For GPU acceleration (NVIDIA CUDA)
pip install torch>=2.0.0 torchvision>=0.15.0 torchaudio>=2.0.0 --index-url https://download.pytorch.org/whl/cu118

# For CPU-only (compatible with all systems)
pip install torch>=2.0.0 torchvision>=0.15.0 torchaudio>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
```

#### Method 3: Using Conda (Alternative)
```cmd
# Download and install Anaconda/Miniconda from anaconda.com

# Create conda environment
conda create -n cctv_env python=3.9 -y
conda activate cctv_env

# Install packages via conda-forge (better compatibility)
conda install -c conda-forge opencv flask numpy ultralytics -y
conda install -c conda-forge yt-dlp psutil requests pillow -y

# Install remaining packages via pip
pip install flask-socketio>=5.3.0 onvif-zeep>=0.2.12 eventlet>=0.33.0
pip install zeep>=4.2.0 pywin32>=306
```

#### Windows-Specific Dependencies and Configuration

**Required System Components:**
```cmd
# For webcam support on Windows
pip install pywin32>=306

# For better video codec support
# Download and install K-Lite Codec Pack from codecguide.com

# Microsoft Visual C++ Redistributable (required for OpenCV)
# Download from: https://aka.ms/vs/17/release/vc_redist.x64.exe
```

**Windows Firewall Configuration:**
```cmd
# Allow Python through Windows Firewall (automatic with install.bat)
netsh advfirewall firewall add rule name="Multi-Source CCTV - Web Interface" dir=in action=allow protocol=TCP localport=4000

# Allow ONVIF discovery ports
netsh advfirewall firewall add rule name="Multi-Source CCTV - ONVIF Discovery" dir=in action=allow protocol=TCP localport=80,8080,8899

# Allow Python application (when prompted by Windows Security)
# Click "Allow access" when Windows Defender prompts
```

**Windows-Specific Features:**
- **📹 DirectShow Webcam Support**: Enhanced webcam detection and control
- **🔧 Windows Service Mode**: Can be configured to run as Windows service
- **🛡️ Windows Defender Integration**: Automatic firewall configuration
- **📁 File Association**: Associate with .cctv project files
- **🚀 Startup Integration**: Auto-start with Windows (optional)

**Troubleshooting Windows Issues:**
```cmd
# If OpenCV installation fails
pip install opencv-python-headless>=4.8.0

# If PyTorch CUDA installation fails
# Check CUDA version: nvidia-smi
# Install appropriate version: https://pytorch.org/get-started/locally/

# If webcam access denied
# Check Windows Privacy Settings > Camera > Allow apps to access camera

# If ONVIF discovery fails
# Disable Windows Firewall temporarily to test
# Check network adapter settings and IP ranges

# If performance issues on Windows
# Disable Windows real-time protection for project folder
# Add Python.exe to Windows Defender exclusions
```

**Performance Optimization for Windows:**
```cmd
# Enable High Performance power plan
powercfg /setactive SCHEME_MIN

# Disable Windows Game Mode (can interfere with video processing)
# Settings > Gaming > Game Mode > Off

# Increase virtual memory (for large video processing)
# Control Panel > System > Advanced > Performance Settings > Virtual Memory

# For NVIDIA GPUs: Install latest drivers and CUDA toolkit
# Download from: https://developer.nvidia.com/cuda-downloads
```

### 📋 Complete Dependencies List

#### Core Requirements (requirements.txt)
```txt
opencv-python>=4.8.0
flask>=2.3.0
flask-socketio>=5.3.0
onvif-zeep>=0.2.12
numpy>=1.24.0
ultralytics>=8.0.0
yt-dlp>=2023.7.6
pathlib>=1.0.1
threading>=3.8.0
```

#### Optional Dependencies
```txt
# For GPU acceleration
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# For better video processing
ffmpeg-python>=0.2.0

# For advanced streaming
streamlink>=5.5.0

# For system monitoring
psutil>=5.9.0

# For configuration management
pyyaml>=6.0
```

### 🔧 Installation Script

#### Linux (install.sh)
```bash
#!/bin/bash
echo "🚀 Installing Multi-Source CCTV System on Linux..."

# Check Python version
python_version=$(python3 -c 'import sys; print(".".join(map(str, sys.version_info[:2])))')
echo "📍 Python version: $python_version"

if [[ $(echo "$python_version < 3.8" | bc -l) -eq 1 ]]; then
    echo "❌ Python 3.8+ required. Current: $python_version"
    exit 1
fi

# Install system dependencies
if command -v apt &> /dev/null; then
    sudo apt update
    sudo apt install -y python3-opencv libopencv-dev ffmpeg libavcodec-extra
elif command -v yum &> /dev/null; then
    sudo yum install -y opencv opencv-python opencv-devel ffmpeg ffmpeg-devel
elif command -v dnf &> /dev/null; then
    sudo dnf install -y opencv opencv-python opencv-devel ffmpeg ffmpeg-devel
fi

# Create virtual environment
python3 -m venv cctv_env
source cctv_env/bin/activate

# Install Python packages
pip install --upgrade pip
pip install -r requirements.txt

echo "✅ Installation completed!"
echo "🚀 Run: source cctv_env/bin/activate && python app.py"
```

#### Windows (install.bat)
```batch
@echo off
echo 🚀 Installing Multi-Source CCTV System on Windows...

REM Check Python installation
python --version >nul 2>&1
if errorlevel 1 (
    echo ❌ Python not found. Please install Python 3.8+ from python.org
    pause
    exit /b 1
)

REM Create virtual environment
python -m venv cctv_env
call cctv_env\Scripts\activate.bat

REM Upgrade pip
python -m pip install --upgrade pip

REM Install requirements
pip install -r requirements.txt

echo ✅ Installation completed!
echo 🚀 Run: cctv_env\Scripts\activate.bat && python app.py
pause
```

### 🚀 Quick Start

#### Windows Quick Start
```cmd
# Method 1: Using Automated Installer (Easiest)
1. Download install.bat from the repository
2. Right-click install.bat and select "Run as administrator"
3. Choose installation type (Quick/GPU/Development/Custom)
4. Wait for installation to complete
5. Double-click start_cctv.bat to launch
6. Open http://localhost:4000 in your browser

# Method 2: Manual Setup
1. Clone the repository: git clone https://github.com/your-repo/multi-source-cctv.git
2. Run install.bat as administrator
3. Launch with start_cctv.bat
```

#### Linux Quick Start
```bash
# Clone repository
git clone https://github.com/your-repo/multi-source-cctv.git
cd multi-source-cctv

# Make install script executable
chmod +x install.sh

# Run installation
./install.sh

# Activate environment and start
source cctv_env/bin/activate
python app.py

# Access web interface
# Open browser to: http://localhost:4000
```

#### Docker Quick Start
```bash
# Pull and run Docker container
docker pull multisource-cctv:latest
docker run -p 4000:4000 -v /path/to/models:/app/models multisource-cctv:latest

# Or build from source
docker build -t multisource-cctv .
docker run -p 4000:4000 multisource-cctv
```

#### First Time Setup Guide

**Step 1: Access Web Interface**
```
Open your browser and navigate to: http://localhost:4000
You should see the Multi-Source CCTV System interface
```

**Step 2: Test with Webcam (Easiest)**
```
1. Click "Webcam" source type
2. Click "Refresh Webcam List"
3. Select your webcam from the list
4. Click "Connect to Source"
5. Enable AI Detection toggle
6. You should see video with person detection
```

**Step 3: Connect IP Camera (RTSP)**
```
1. Click "RTSP/IP" source type
2. Enter your camera's RTSP URL:
   - Basic: rtsp://192.168.1.100:554/stream1
   - With Auth: rtsp://admin:password@192.168.1.100:554/stream1
3. Enter camera IP for PTZ control (optional)
4. Click "Connect to Source"
5. Enable AI Detection and PTZ tracking
```

**Step 4: Try Live Streaming**
```
1. Click "Live Stream" source type
2. Use a template or enter URL:
   - YouTube Live: https://www.youtube.com/watch?v=LIVE_VIDEO_ID
   - HLS Stream: https://example.com/live/stream.m3u8
3. Enable "Preserve Original Timing"
4. Click "Connect to Source"
```

**Step 5: Load Video File**
```
1. Click "File/URL" source type
2. Enter file path or URL:
   - Local: C:\Videos\sample.mp4
   - Network: http://example.com/video.mp4
3. Enable "Preserve Original Speed"
4. Choose "Loop Video" if desired
5. Click "Connect to Source"
```

### 🔍 Verification

#### Test Installation
```bash
# Activate environment
source cctv_env/bin/activate  # Linux
# OR
cctv_env\Scripts\activate.bat  # Windows

# Test imports
python -c "import cv2; print('✅ OpenCV:', cv2.__version__)"
python -c "import flask; print('✅ Flask:', flask.__version__)"
python -c "import ultralytics; print('✅ Ultralytics available')"
python -c "import yt_dlp; print('✅ yt-dlp available')"

# Test YOLO model download
python -c "from ultralytics import YOLO; model = YOLO('yolov8n.pt'); print('✅ YOLO model loaded')"
```

### 📦 Pre-built Releases

For users who prefer not to install dependencies manually:

#### Windows Executable (Coming Soon)
- Standalone .exe file with all dependencies
- No Python installation required
- Portable version available

#### Docker Container
```bash
# Pull Docker image
docker pull multisource-cctv:latest

# Run container
docker run -p 4000:4000 -v /path/to/models:/app/models multisource-cctv:latest
```

### 🎯 Post-Installation Setup

1. **Download YOLO Models** (Optional - auto-downloads on first use)
```bash
mkdir models
cd models
wget https://github.com/ultralytics/assets/releases/download/v8.2.0/yolov8n.pt
wget https://github.com/ultralytics/assets/releases/download/v8.2.0/yolo11n.pt
```

2. **Configure Firewall** (Windows)
- Allow Python through Windows Firewall
- Open port 4000 for web interface
- Allow ONVIF discovery (ports 80, 8080, 8899)

3. **Test Camera Connection**
- RTSP: Test with VLC player first
- Webcam: Check with built-in camera app
- Network: Verify camera accessibility

4. **Access Web Interface**
- Open browser to http://localhost:4000
- Select video source and connect
- Verify video stream and AI detection

## 📦 AI Model Support

### Official YOLO Models (Auto-Download)
- **YOLO11**: yolo11n.pt, yolo11s.pt, yolo11m.pt, yolo11l.pt, yolo11x.pt
- **YOLOv8**: yolov8n.pt, yolov8s.pt, yolov8m.pt, yolov8l.pt, yolov8x.pt
- **YOLOv5**: yolov5n.pt, yolov5s.pt, yolov5m.pt, yolov5l.pt, yolov5x.pt

### Custom Models
- **Any .pt File**: my_model.pt, best.pt, custom_weights.pt, etc.
- **Smart Priority System**: Official models > Custom models > Size-based selection
- **Size Filtering**: 1MB-500MB (excludes corrupted files)
- **Pattern Matching**: Prioritizes YOLO-like named files

### Model Discovery Locations
```
./models/
./weights/
./
~/.cache/ultralytics/
/usr/local/share/ultralytics/ (Linux)
C:/ProgramData/ultralytics/ (Windows)
```

## 🔧 Configuration

### RTSP Camera Setup
```python
# Basic RTSP
rtsp://192.168.1.100:554/stream1

# With Authentication in URL
rtsp://admin:password@192.168.1.100:554/stream1

# Brand-Specific Examples
# Hikvision
rtsp://admin:admin@192.168.1.100:554/Streaming/Channels/101

# Dahua
rtsp://admin:admin@192.168.1.100:554/cam/realmonitor?channel=1&subtype=0

# Axis
rtsp://admin:admin@192.168.1.100:554/axis-media/media.amp
```

### Live Stream URLs
```python
# YouTube Live
https://www.youtube.com/watch?v=VIDEO_ID

# Twitch
https://www.twitch.tv/CHANNEL_NAME

# HLS Live Stream
https://example.com/live/stream.m3u8

# Facebook Live
https://www.facebook.com/USERNAME/live
```

### File Sources
```python
# Local Files
/path/to/video.mp4

# Network Files
http://example.com/video.mp4

# With Loop
loop_video: true
```

## 🎛️ API Endpoints

### Connection Management
- `POST /connect_source` - Connect to video source
- `POST /disconnect` - Disconnect current source
- `GET /scan_webcams` - Discover available webcams

### AI Controls
- `POST /toggle_yolo` - Enable/disable YOLO detection
- `POST /toggle_detection_overlay` - Show/hide detection boxes
- `POST /update_settings` - Update AI parameters

### PTZ Control
- `POST /ptz_move` - Move PTZ camera
- `POST /toggle_tracking` - Enable/disable auto-tracking

### Streaming
- `GET /video_feed` - Real-time video stream
- `GET /status` - System status and performance

## 🔍 Detection Features

### Object Detection
- **80+ Object Classes**: Standard COCO dataset objects
- **Person Detection**: Specialized person tracking and counting
- **Confidence Threshold**: Adjustable detection sensitivity
- **Input Size**: Configurable for speed vs accuracy balance

### Visual Feedback
- **Golden Bounding Boxes**: High-contrast detection visualization
- **Object Labels**: Class names with confidence scores
- **Detection Overlay Toggle**: Hide boxes while keeping detection active
- **Real-time Counts**: Live person counting display

## 📊 Performance Optimization

### Speed Settings
- **Target FPS**: 10-30 FPS range
- **Quality Modes**: High, Balanced, Performance, Auto
- **Input Sizes**: 320×320 (Fast), 416×416 (Balanced), 640×640 (Accurate)
- **Buffer Management**: Minimal latency configuration

### Hardware Acceleration
- **GPU Support**: CUDA acceleration when available
- **CPU Fallback**: Optimized CPU processing
- **Threading**: Multi-threaded capture and processing

## 🔒 Security Features

### Authentication
- **RTSP Credentials**: Username/password support
- **URL Embedded Auth**: Parse credentials from RTSP URLs
- **ONVIF Discovery**: Multiple authentication methods
- **Fallback Options**: Graceful degradation for auth failures

### Network Security
- **CORS Support**: Cross-origin resource sharing
- **Timeout Controls**: Connection and read timeouts
- **Error Handling**: Graceful failure management

## 🐛 Troubleshooting

### Common Issues and Solutions

#### Installation Issues

**❌ Python Not Found**
```bash
# Linux
sudo apt install python3 python3-pip python3-venv

# Windows
# Download Python from https://python.org/downloads/
# Make sure to check "Add Python to PATH"
```

**❌ Permission Denied (Linux)**
```bash
# Don't use sudo with pip, use virtual environment instead
python3 -m venv cctv_env
source cctv_env/bin/activate
pip install -r requirements.txt
```

**❌ Microsoft Visual C++ Build Tools Required (Windows)**
```cmd
# Download and install from:
# https://visualstudio.microsoft.com/visual-cpp-build-tools/
# Or install Visual Studio Community with C++ tools
```

**❌ OpenCV Installation Failed**
```bash
# Try headless version
pip install opencv-python-headless>=4.8.0

# Or install system packages first (Linux)
sudo apt install libopencv-dev python3-opencv

# Clear pip cache and retry
pip cache purge
pip install --no-cache-dir opencv-python
```

**❌ PyTorch CUDA Issues**
```bash
# Check CUDA version
nvidia-smi

# Install appropriate PyTorch version
# CUDA 11.8
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121

# CPU-only (fallback)
pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# Verify installation
python -c "import torch; print('CUDA:', torch.cuda.is_available())"
```

#### Connection Issues

**❌ RTSP Connection Failed**
```bash
# Test RTSP URL with VLC first
vlc rtsp://192.168.1.100:554/stream1

# Common RTSP URL formats:
# Hikvision: rtsp://admin:password@IP:554/Streaming/Channels/101
# Dahua: rtsp://admin:password@IP:554/cam/realmonitor?channel=1&subtype=0
# Axis: rtsp://admin:password@IP:554/axis-media/media.amp

# Check credentials and network connectivity
ping 192.168.1.100
telnet 192.168.1.100 554
```

**❌ Webcam Not Detected**
```bash
# Linux - check camera permissions
sudo usermod -a -G video $USER
logout  # and login again

# Check available cameras
ls /dev/video*

# Test with v4l2
v4l2-ctl --list-devices

# Windows - check privacy settings
# Settings > Privacy > Camera > Allow apps to access camera
```

**❌ ONVIF Discovery Failed**
```bash
# Check firewall settings
# Linux
sudo ufw allow 80,8080,8899/tcp

# Windows
netsh advfirewall firewall add rule name="ONVIF" dir=in action=allow protocol=TCP localport=80,8080,8899

# Test manual ONVIF connection
# Try different ports: 80, 8080, 8899, 8000, 554
```

**❌ YouTube/Twitch Stream Issues**
```bash
# Update yt-dlp
pip install --upgrade yt-dlp

# Clear yt-dlp cache
yt-dlp --rm-cache-dir

# Test stream URL manually
yt-dlp --get-url "https://www.youtube.com/watch?v=VIDEO_ID"

# For region-restricted content, use VPN
```

#### Performance Issues

**❌ Low FPS / High Latency**
```bash
# Reduce YOLO input size
# Settings > AI Detection > Input Size: 320x320

# Lower confidence threshold
# Settings > AI Detection > Confidence: 30%

# Disable detection overlay temporarily
# Toggle "Show Detection Boxes" off

# Use CPU instead of GPU for small models
export CUDA_VISIBLE_DEVICES=""
```

**❌ High CPU Usage**
```bash
# Check system resources
htop  # Linux
# Task Manager # Windows

# Reduce target FPS
# Settings > Performance > Target FPS: 15

# Use smaller YOLO model
# Download yolov8n.pt instead of yolov8l.pt

# Enable GPU acceleration if available
nvidia-smi  # Check GPU status
```

**❌ Memory Issues**
```bash
# Monitor memory usage
free -h  # Linux
# Task Manager > Performance > Memory  # Windows

# Reduce buffer sizes
# Settings > choose smaller buffer sizes

# Restart application periodically for long-running deployments
```

#### Video Quality Issues

**❌ Poor Video Quality**
```bash
# Increase quality settings
# Settings > Performance > Quality Mode: High

# Check source resolution
# Verify camera/stream settings

# Ensure adequate bandwidth for network streams
speedtest-cli
```

**❌ Video Stuttering/Freezing**
```bash
# Check network connectivity
ping -c 10 CAMERA_IP

# Test with different buffer sizes
# File sources: Large buffer
# Live streams: Minimal buffer

# Verify codec compatibility
ffprobe VIDEO_FILE  # Check video format
```

#### AI Detection Issues

**❌ YOLO Model Not Loading**
```bash
# Check models directory
ls models/
# Should contain .pt files

# Download models manually
wget https://github.com/ultralytics/assets/releases/download/v8.2.0/yolov8n.pt -P models/

# Test model loading
python -c "from ultralytics import YOLO; YOLO('models/yolov8n.pt')"

# Check for corrupted downloads
# Re-download if file size is too small
```

**❌ No Objects Detected**
```bash
# Lower confidence threshold
# Settings > AI Detection > Confidence: 20%

# Check lighting conditions
# YOLO works better with good lighting

# Try different model sizes
# yolov8s.pt or yolov8m.pt for better accuracy

# Verify camera angle and distance
# Objects should be clearly visible
```

**❌ False Detections**
```bash
# Increase confidence threshold
# Settings > AI Detection > Confidence: 60%

# Use larger input size
# Settings > AI Detection > Input Size: 640x640

# Check for reflections or shadows
# Adjust camera positioning
```

#### Network and Firewall Issues

**❌ Can't Access Web Interface**
```bash
# Check if service is running
netstat -tlnp | grep 4000  # Linux
netstat -an | findstr 4000  # Windows

# Test local access
curl http://localhost:4000

# Check firewall rules
# Linux
sudo ufw status
sudo ufw allow 4000

# Windows
netsh advfirewall show allprofiles
netsh advfirewall firewall add rule name="CCTV" dir=in action=allow protocol=TCP localport=4000

# Test from another device
# http://YOUR_SERVER_IP:4000
```

**❌ CORS Errors**
```bash
# Check browser console for errors
# Press F12 > Console tab

# Try different browser
# Chrome, Firefox, Edge

# Clear browser cache and cookies
# Ctrl+Shift+Delete

# Access using IP instead of localhost
# http://192.168.1.100:4000
```

#### Advanced Troubleshooting

**🔍 Enable Debug Logging**
```python
# Add to app.py
import logging
logging.basicConfig(level=logging.DEBUG)

# Or set environment variable
export FLASK_ENV=development  # Linux
set FLASK_ENV=development     # Windows
```

**🔍 Check System Resources**
```bash
# Linux
htop
iostat -x 1
nvidia-smi -l 1  # For GPU monitoring

# Windows
# Task Manager > Performance tab
# Resource Monitor > Network/Disk tabs
```

**🔍 Network Diagnostics**
```bash
# Test camera connectivity
nmap -p 554,80,8080 CAMERA_IP

# Check bandwidth
iperf3 -c CAMERA_IP  # If supported

# Monitor network traffic
tcpdump -i any port 554  # Linux
# Wireshark on Windows
```

**🔍 Application Logs**
```bash
# Run with verbose output
python app.py --verbose

# Check system logs
# Linux
journalctl -u cctv-service  # If running as service
tail -f /var/log/syslog

# Windows
# Event Viewer > Application logs
```

### Getting Help

**📧 Before Reporting Issues:**
1. Check this troubleshooting guide
2. Search existing GitHub issues
3. Test with minimal configuration
4. Gather system information

**📋 Include in Bug Reports:**
- Operating system and version
- Python version (`python --version`)
- Complete error messages
- Steps to reproduce
- System specifications
- Network configuration (for connection issues)

**🔗 Support Resources:**
- GitHub Issues: Report bugs and feature requests
- Documentation: Check README.md for detailed guides
- Community Forum: Share experiences and solutions
- Video Tutorials: Step-by-step setup guides

**⚡ Quick Health Check:**
```bash
# Test all components
python -c "
import cv2, flask, torch, ultralytics
print('✅ All imports successful')
print(f'OpenCV: {cv2.__version__}')
print(f'PyTorch: {torch.__version__}')
print(f'CUDA: {torch.cuda.is_available()}')
"

# Test camera access
python -c "
import cv2
cap = cv2.VideoCapture(0)
ret, frame = cap.read()
print('✅ Camera test:', 'OK' if ret else 'FAILED')
cap.release()
"

# Test YOLO model
python -c "
from ultralytics import YOLO
model = YOLO('yolov8n.pt')
print('✅ YOLO model loaded successfully')
"
```

## 📁 Project Structure

```
multi-source-cctv/
├── 📄 app.py                    # Main application server
├── 🌐 index.html               # Web interface frontend
├── 📋 requirements.txt         # Python dependencies
├── 📖 README.md                # This documentation
├── 🪟 install.bat              # Windows installation script
├── 🐧 install.sh               # Linux installation script
├── 🚀 start_cctv.bat           # Windows startup script (auto-generated)
├── 🔄 update_system.bat        # Windows update script (auto-generated)
├── 📁 models/                  # YOLO model directory
│   ├── yolov8n.pt             # Nano model (fastest)
│   ├── yolov8s.pt             # Small model (balanced)
│   ├── yolov8m.pt             # Medium model (accurate)
│   └── custom_model.pt        # Custom trained models
├── 📁 cctv_env/               # Virtual environment (auto-generated)
│   ├── Scripts/               # Windows executables
│   ├── bin/                   # Linux executables
│   └── lib/                   # Python packages
├── 📁 logs/                   # Application logs (optional)
│   ├── cctv_system.log        # Main application log
│   ├── detection.log          # AI detection log
│   └── performance.log        # Performance metrics
├── 📁 config/                 # Configuration files (optional)
│   ├── cameras.json           # Saved camera configurations
│   ├── settings.json          # User preferences
│   └── templates.json         # RTSP URL templates
├── 📁 static/                 # Web assets (optional)
│   ├── css/                   # Custom styles
│   ├── js/                    # Additional scripts
│   └── images/                # Icons and graphics
├── 📁 templates/              # Additional HTML templates (optional)
│   ├── mobile.html            # Mobile-optimized interface
│   └── admin.html             # Administrative interface
├── 📁 utils/                  # Utility modules (optional)
│   ├── camera_utils.py        # Camera helper functions
│   ├── onvif_utils.py         # ONVIF utilities
│   └── stream_utils.py        # Streaming utilities
├── 📁 tests/                  # Test suite (development)
│   ├── test_detection.py      # AI detection tests
│   ├── test_streaming.py      # Streaming tests
│   └── test_onvif.py          # PTZ control tests
├── 📄 Dockerfile             # Docker container setup
├── 📄 docker-compose.yml     # Multi-container setup
├── 📄 .gitignore             # Git ignore rules
├── 📄 LICENSE                # Software license
└── 📄 CHANGELOG.md           # Version history
```

### Core Files Description

**🔧 Main Application Files:**
- `app.py` - Flask server with multi-source video processing, AI detection, and PTZ control
- `index.html` - Single-page web interface with real-time video streaming and controls
- `requirements.txt` - Complete Python package dependencies with version specifications

**🚀 Installation & Setup:**
- `install.bat` - Windows automated installer with GUI options and firewall configuration
- `install.sh` - Linux installation script with distribution-specific package management
- `start_cctv.bat` - Windows startup script (auto-generated by installer)
- `update_system.bat` - Windows dependency updater (auto-generated by installer)

**🤖 AI Models Directory:**
- `models/` - YOLO model storage with auto-detection and priority system
- Supports official models (yolo11n.pt, yolov8n.pt, etc.) and custom .pt files
- Auto-downloads missing models on first use
- Smart model selection based on performance requirements

**⚙️ Configuration System:**
- `config/cameras.json` - Saved RTSP camera configurations with credentials
- `config/settings.json` - User preferences and performance settings
- `config/templates.json` - RTSP URL templates for different camera brands

**📊 Logging & Monitoring:**
- `logs/cctv_system.log` - Main application events and errors
- `logs/detection.log` - AI detection results and performance metrics
- `logs/performance.log` - System resource usage and optimization data

### Auto-Generated Structure

When you run the installation, these directories and files are automatically created:

**After Windows Installation:**
```
multi-source-cctv/
├── ✅ cctv_env/              # Python virtual environment
├── ✅ models/                # YOLO models directory
├── ✅ requirements.txt       # Generated dependency list
├── ✅ start_cctv.bat        # Windows startup script
├── ✅ update_system.bat     # Windows update script
└── ✅ config/               # Configuration directory (on first run)
```

**After Linux Installation:**
```
multi-source-cctv/
├── ✅ cctv_env/              # Python virtual environment
├── ✅ models/                # YOLO models directory
├── ✅ requirements.txt       # Generated dependency list
├── ✅ start_cctv.sh         # Linux startup script
├── ✅ update_system.sh      # Linux update script
└── ✅ config/               # Configuration directory (on first run)
```

### Development Structure

For contributors and developers, additional files may be present:

**🧪 Testing & Development:**
```
multi-source-cctv/
├── 📁 tests/                 # Automated test suite
│   ├── conftest.py           # Test configuration
│   ├── test_app.py           # Main application tests
│   ├── test_detection.py     # AI detection tests
│   ├── test_streaming.py     # Video streaming tests
│   ├── test_onvif.py         # PTZ control tests
│   └── fixtures/             # Test data and mock objects
├── 📁 docs/                  # Documentation source
│   ├── api.md                # API documentation
│   ├── deployment.md         # Deployment guide
│   └── development.md        # Development setup
├── 📄 .github/               # GitHub workflows
│   ├── workflows/            # CI/CD pipelines
│   └── ISSUE_TEMPLATE.md     # Issue templates
├── 📄 pyproject.toml         # Python project configuration
├── 📄 setup.py               # Package setup script
└── 📄 tox.ini                # Testing configuration
```

### Directory Permissions

**Linux Permissions:**
```bash
# Required permissions for proper operation
chmod +x install.sh start_cctv.sh update_system.sh
chmod 755 models/ config/ logs/
chmod 644 requirements.txt README.md
chmod 600 config/cameras.json  # Sensitive camera credentials
```

**Windows Permissions:**
- Installer requires Administrator privileges for firewall configuration
- Models directory needs write access for auto-downloads
- Config directory needs write access for settings storage

### Storage Requirements

**Disk Space:**
- **Base Installation**: ~500MB (Python packages and dependencies)
- **YOLO Models**: 6MB (nano) to 166MB (extra-large) per model
- **Logs**: 1-10MB per day (depending on activity)
- **Configuration**: <1MB (camera settings and templates)
- **Virtual Environment**: 200-300MB (isolated Python packages)

**Recommended Free Space**: 2-5GB for comfortable operation with multiple models

### Backup Recommendations

**Critical Files to Backup:**
```
📁 config/cameras.json        # Camera configurations and credentials
📁 config/settings.json       # User preferences and optimizations
📁 models/*.pt               # Custom trained models (if any)
📁 logs/                     # Historical data and troubleshooting info
📄 requirements.txt          # Exact dependency versions
```

**Backup Script Example:**
```bash
# Linux backup script
tar -czf cctv_backup_$(date +%Y%m%d).tar.gz config/ models/ logs/ requirements.txt

# Windows backup (PowerShell)
Compress-Archive -Path config,models,logs,requirements.txt -DestinationPath "cctv_backup_$(Get-Date -Format 'yyyyMMdd').zip"
```

## 🚢 Deployment

### 🏠 Local Development

**Quick Development Setup:**
```bash
# Clone and setup
git clone https://github.com/your-repo/multi-source-cctv.git
cd multi-source-cctv

# Install in development mode
pip install -e .
pip install -r requirements-dev.txt

# Run with hot reload
export FLASK_ENV=development
python app.py
```

### 🐳 Docker Deployment

**Basic Docker Setup:**
```dockerfile
# Dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .
EXPOSE 4000

CMD ["python", "app.py"]
```

**Build and Run:**
```bash
# Build image
docker build -t multisource-cctv .

# Run container
docker run -p 4000:4000 -v ./models:/app/models multisource-cctv

# With GPU support (NVIDIA)
docker run --gpus all -p 4000:4000 -v ./models:/app/models multisource-cctv
```

**Docker Compose:**
```yaml
# docker-compose.yml
version: '3.8'
services:
  cctv:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - ./models:/app/models
      - ./config:/app/config
      - ./logs:/app/logs
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
    
  # Optional: Add database for configuration storage
  redis:
    image: redis:alpine
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data:
```

### ☁️ Cloud Deployment

**AWS EC2 Deployment:**
```bash
# Launch EC2 instance (t3.medium recommended)
# Install Docker and docker-compose
sudo yum update -y
sudo yum install -y docker
sudo systemctl start docker
sudo usermod -a -G docker ec2-user

# Clone and deploy
git clone https://github.com/your-repo/multi-source-cctv.git
cd multi-source-cctv
docker-compose up -d

# Configure security group for port 4000
# AWS Console > EC2 > Security Groups > Add inbound rule
# Type: Custom TCP, Port: 4000, Source: Your IP or 0.0.0.0/0
```

**Google Cloud Platform:**
```bash
# Use Google Cloud Run for serverless deployment
gcloud builds submit --tag gcr.io/PROJECT_ID/multisource-cctv
gcloud run deploy --image gcr.io/PROJECT_ID/multisource-cctv --platform managed
```

**Azure Container Instances:**
```bash
# Deploy to Azure
az container create \
  --resource-group myResourceGroup \
  --name multisource-cctv \
  --image yourdockerhub/multisource-cctv \
  --ports 4000 \
  --environment-variables FLASK_ENV=production
```

### 🖥️ Server Deployment

**Ubuntu Server (Systemd Service):**
```bash
# Install system dependencies
sudo apt update
sudo apt install python3 python3-pip python3-venv nginx

# Create service user
sudo useradd -m -s /bin/bash cctv
sudo su - cctv

# Install application
git clone https://github.com/your-repo/multi-source-cctv.git
cd multi-source-cctv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Create systemd service
sudo tee /etc/systemd/system/multisource-cctv.service << EOF
[Unit]
Description=Multi-Source CCTV System
After=network.target

[Service]
Type=simple
User=cctv
WorkingDirectory=/home/cctv/multi-source-cctv
Environment=PATH=/home/cctv/multi-source-cctv/venv/bin
ExecStart=/home/cctv/multi-source-cctv/venv/bin/python app.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# Enable and start service
sudo systemctl daemon-reload
sudo systemctl enable multisource-cctv
sudo systemctl start multisource-cctv
```

**Nginx Reverse Proxy:**
```nginx
# /etc/nginx/sites-available/multisource-cctv
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:4000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # WebSocket support
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

# Enable site
sudo ln -s /etc/nginx/sites-available/multisource-cctv /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

**SSL with Let's Encrypt:**
```bash
# Install certbot
sudo apt install certbot python3-certbot-nginx

# Get SSL certificate
sudo certbot --nginx -d your-domain.com

# Auto-renewal
sudo crontab -e
# Add: 0 12 * * * /usr/bin/certbot renew --quiet
```

### 🌐 Production Configuration

**Environment Variables:**
```bash
# Production settings
export FLASK_ENV=production
export SECRET_KEY=your-secret-key-here
export DATABASE_URL=redis://localhost:6379
export MAX_CAMERAS=10
export LOG_LEVEL=INFO
export YOLO_DEVICE=cuda  # or cpu
export STREAM_QUALITY=80
export ENABLE_GPU=true
```

**Production Requirements:**
```txt
# requirements-prod.txt
gunicorn>=21.0.0
redis>=4.5.0
psutil>=5.9.0
prometheus-client>=0.16.0
sentry-sdk>=1.32.0
```

**Gunicorn Configuration:**
```python
# gunicorn.conf.py
bind = "0.0.0.0:4000"
workers = 4
worker_class = "eventlet"
worker_connections = 1000
max_requests = 1000
max_requests_jitter = 100
timeout = 30
keepalive = 2
preload_app = True
accesslog = "/var/log/cctv/access.log"
errorlog = "/var/log/cctv/error.log"
loglevel = "info"
```

### 📊 Monitoring and Logging

**Application Monitoring:**
```python
# Add to app.py for production monitoring
from prometheus_client import Counter, Histogram, generate_latest
import time

# Metrics
REQUEST_COUNT = Counter('requests_total', 'Total requests', ['method', 'endpoint'])
REQUEST_LATENCY = Histogram('request_duration_seconds', 'Request latency')

@app.before_request
def before_request():
    request.start_time = time.time()

@app.after_request
def after_request(response):
    REQUEST_COUNT.labels(method=request.method, endpoint=request.endpoint).inc()
    REQUEST_LATENCY.observe(time.time() - request.start_time)
    return response

@app.route('/metrics')
def metrics():
    return Response(generate_latest(), mimetype='text/plain')
```

**Log Configuration:**
```python
# Enhanced logging for production
import logging
from logging.handlers import RotatingFileHandler

if not app.debug:
    file_handler = RotatingFileHandler(
        '/var/log/cctv/app.log', 
        maxBytes=10240000, 
        backupCount=10
    )
    file_handler.setFormatter(logging.Formatter(
        '%(asctime)s %(levelname)s: %(message)s [in %(pathname)s:%(lineno)d]'
    ))
    file_handler.setLevel(logging.INFO)
    app.logger.addHandler(file_handler)
    app.logger.setLevel(logging.INFO)
```

### 🔒 Security Considerations

**Production Security Checklist:**
- ✅ Use HTTPS with valid SSL certificates
- ✅ Implement authentication and authorization
- ✅ Set up firewall rules (only necessary ports)
- ✅ Regular security updates
- ✅ Monitor access logs
- ✅ Use environment variables for secrets
- ✅ Enable CSRF protection
- ✅ Implement rate limiting

**Firewall Configuration:**
```bash
# UFW (Ubuntu)
sudo ufw default deny incoming
sudo ufw default allow outgoing
sudo ufw allow ssh
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw allow 4000/tcp  # If direct access needed
sudo ufw enable

# iptables (manual)
iptables -A INPUT -p tcp --dport 80 -j ACCEPT
iptables -A INPUT -p tcp --dport 443 -j ACCEPT
iptables -A INPUT -p tcp --dport 4000 -j ACCEPT
```

### 🔄 Backup and Recovery

**Automated Backup Script:**
```bash
#!/bin/bash
# backup.sh
DATE=$(date +%Y%m%d_%H%M%S)
BACKUP_DIR="/backup/cctv/$DATE"

mkdir -p $BACKUP_DIR

# Backup configuration
cp -r /home/cctv/multi-source-cctv/config $BACKUP_DIR/
cp -r /home/cctv/multi-source-cctv/models $BACKUP_DIR/
cp /home/cctv/multi-source-cctv/requirements.txt $BACKUP_DIR/

# Backup logs (last 7 days)
find /var/log/cctv -mtime -7 -exec cp {} $BACKUP_DIR/ \;

# Compress
tar -czf /backup/cctv_backup_$DATE.tar.gz -C /backup/cctv $DATE
rm -rf $BACKUP_DIR

# Cleanup old backups (keep 30 days)
find /backup -name "cctv_backup_*.tar.gz" -mtime +30 -delete

echo "Backup completed: /backup/cctv_backup_$DATE.tar.gz"
```

**Recovery Procedure:**
```bash
# Stop service
sudo systemctl stop multisource-cctv

# Restore from backup
cd /home/cctv
tar -xzf /backup/cctv_backup_20231201_120000.tar.gz
cp -r 20231201_120000/config multi-source-cctv/
cp -r 20231201_120000/models multi-source-cctv/

# Restart service
sudo systemctl start multisource-cctv
```

### 📈 Performance Optimization

**Production Performance Tips:**
1. **Use GPU acceleration** for YOLO inference
2. **Optimize buffer sizes** based on network conditions
3. **Enable connection pooling** for multiple cameras
4. **Use CDN** for static assets
5. **Implement caching** for frequently accessed data
6. **Monitor resource usage** and scale accordingly

**Load Balancing (Multiple Instances):**
```nginx
# nginx load balancing
upstream cctv_backend {
    server 127.0.0.1:4000;
    server 127.0.0.1:4001;
    server 127.0.0.1:4002;
}

server {
    location / {
        proxy_pass http://cctv_backend;
    }
}
```

### 🚨 Troubleshooting Production Issues

**Common Production Problems:**
1. **High memory usage** - Monitor model loading and frame processing
2. **Network timeouts** - Adjust camera connection timeouts
3. **SSL certificate issues** - Check certificate renewal
4. **Permission errors** - Verify file/directory permissions
5. **Database connection** - Check Redis/database connectivity

**Health Check Endpoint:**
```python
@app.route('/health')
def health_check():
    return jsonify({
        'status': 'healthy',
        'timestamp': time.time(),
        'version': '1.0.0',
        'cameras_connected': len(active_cameras),
        'gpu_available': torch.cuda.is_available()
    })
```

## 🙏 Acknowledgments

- **Ultralytics**: YOLO implementation
- **OpenCV**: Computer vision library
- **Flask**: Web framework
- **ONVIF**: PTZ camera control standard

## 📧 Support

For issues and questions:
1. Check the troubleshooting section
2. Search existing issues
3. Create a new issue with detailed description
4. Include system information and error logs

---

**⚡ Built for real-time performance with professional-grade features**

![506238882_4196252233936638_4386220351926044013_n](https://github.com/user-attachments/assets/8a9f3c07-7bc6-476c-841e-f7566ac34588)
![FireShot Capture 079 - Multi-Source CCTV System - YOLOv8 AI with Fixed File Timing - localhost](https://github.com/user-attachments/assets/abe77f94-e9a3-4517-926a-1e76f2b597f2)
